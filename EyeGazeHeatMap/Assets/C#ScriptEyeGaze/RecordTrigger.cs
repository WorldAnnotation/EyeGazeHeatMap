/*
using UnityEngine;

public class RecordTrigger : MonoBehaviour
{
    public YourExistingLoggerScript loggerScript; // 既存のログ収集スクリプトの参照

    void Update()
    {
        if (Input.GetKeyDown(KeyCode.R)) // "R" キーが押されたかチェック
        {
            Debug.Log("on record");
            // ここで既存のログ収集スクリプトのメソッドを呼び出すことができます。
            // 例: loggerScript.StartRecording();
        }
    }
}
*/